
(function (window) {

    function quick() {
        const quickApi = { struct, };
        return quickApi;
    }

    if (typeof (window.qk) === 'undefined') {
        window.qk = quick();
    }

})(window);